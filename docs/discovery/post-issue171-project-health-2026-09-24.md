# Post-#171 Project Health Check — 2026-09-24

Issue: #171  
Decision: **CONTINUE — #174 NEXT**

## What changed

#171 supports a new DERIVATION failure family:

`TECHNICAL_STANDARD_AUTHORITY_HANDOFF`

Two independent exposed EBA→Commission RTS chains show the same mechanism:

- an ESA can complete and publish a **final draft RTS**;
- the European Commission can then exercise formal amendment/adoption authority;
- a material operative proposition can change;
- the later Commission Delegated Regulation owns binding legal effect.

The corpus therefore grows from:

- 27 → 29 cases;
- 14 → 15 trap classes.

No evaluation claim changed.

## Why the new class earns itself

The evidence survived first-refusal testing against:

- `PARALLEL_INSTRUMENT_LIFECYCLE`;
- `STATUS_APPLICATION_SEPARATION`;
- `DYNAMIC_REFERENCE_STATUS`;
- `SOURCE_STATE_BACKPROJECTION`.

Each loses a consequential distinction if stretched to own the RTS chains.

The new family is therefore not an alias.

## What did not change

The project identity remains:

> adversarial legal-research corpus + evaluation protocol.

#171 did not establish:

- latent detection;
- Method advantage;
- product value;
- need for lifecycle infrastructure.

## Active-discovery decision

Sponsor direction remains active evidence-driven discovery.

The remaining Cycle 2 reserve with highest information gain is Run C:

> `OFFICIAL_DERIVED_VIEW_LAG`

Unlike #171, the next experiment should use fresh validation cases because Run C
already exposed the motivating sanctions/revised-legislation examples.

Select:

> **#174 — official derived-view lag generality**

## Why #174 now

#174 tests whether a proven narrow class:

`OFFICIAL_TRACKER_UPDATE_LAG`

is actually one instance of a broader mechanism spanning materially different
official derived views.

This can produce either useful expansion or useful subtraction:

- SUPPORT → broader class earned;
- REVISE → tracker class may need a small definition change;
- REJECT → keep #93 narrow and stop generalising.

## Direction

# **CONTINUE**

WIP remains 1.

#174 becomes active only after #171 merges.

No Cycle 3 ideation is needed while an already-earned reserve remains.
