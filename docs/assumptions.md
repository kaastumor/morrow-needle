# Morrow // Needle — Active assumptions

Status: **LIVE REGISTER**

Keep this file small. It contains only material beliefs that can change current
architecture or project direction. Detailed evidence belongs in issues, audits,
fixtures, tests and decision records.

Allowed dispositions: **survives / revise / reject / park / experiment**.

| ID | Assumption | Current disposition | Strongest pressure / discriminating evidence |
|---|---|---|---|
| H-01 | Needle's extra value lies beyond ordinary EU change monitoring, in evidence-typed and temporally explicit legal-state distinctions. | experiment | emendrix substantially overlaps the core watch → diff → corroborate → explain loop. #29 must test each claimed differentiator independently. |
| H-02 | Human-readable Source Mode resolution over existing provenance is the smallest next product improvement. | experiment | Product checkpoint found it across all three real card classes, but #29 must test whether it improves understanding enough to matter versus direct official links. |
| H-03 | A file/GitHub-backed operational pilot is sufficient for the current learning horizon; a database, queue or object store is not yet justified. | survives | Stateful monitor growth/concurrency and provenance-retention attacks must continue. Infrastructure is added only if this baseline fails a concrete requirement. |
| H-04 | Bounded deterministic authentic-instruction parsing can safely verify some amendments before consolidated comparators exist. | revise | #29 found multiple context/locality defects. Source-local segmentation is now required; same-stream quotation remains a bounded residual risk until structural authority is tested. |
| H-05 | Positive non-impact and abstention are useful product states rather than internal errors. | survives | Real metadata-only and unresolved operational cards were understandable in the first product checkpoint; broader user-value evidence is still thin. |
| H-06 | Existing P0/P1 domain contracts are mostly sound and defects can be fixed at adapter/enforcement boundaries. | survives | #29 has found several high-severity executable defects but, so far, no need for broad canonical schema redesign. A single counterexample can reopen a freeze. |

## Register rules

- Add an assumption only if being wrong could change architecture, project
  direction, or interpretation of evidence.
- Promotion requires a defined claim, a credible alternative, discriminating
  evidence, and understood limitations.
- A belief that no longer affects decisions is deleted rather than archived
  forever.
- A rejected assumption must update the backlog/decision it previously
  supported; do not keep zombie roadmap items alive.
